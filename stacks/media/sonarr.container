# ==============================================================================
# Sonarr: TV shows library manager
# ------------------------------------------------------------------------------
# On net-arr to be reached by Caddy and to be polled by Unpackerr and
# Jellyseerr, and on net-download to push releases to qBittorrent - which it
# addresses as torrent:${PORT_QBITTORRENT_WEB}. The pod's infra container holds
# the network and answers to the POD name; neither "gluetun" nor "qbittorrent"
# resolves. That setting lives in Sonarr's database, not here.
#
# It reaches its indexers through Prowlarr on net-arr; the 30-odd Torznab
# entries in its database all point at http://prowlarr:9696/<id>/.
#
# Image:  https://docs.linuxserver.io/images/docker-sonarr
# ==============================================================================

[Unit]
Description=Sonarr

# See pocket-id.container: Restart=always at RestartSec=5 can never reach
# systemd's shipped 5-in-10s ceiling, so a hopeless unit loops for ever instead
# of coming to rest in `failed`.
StartLimitIntervalSec=600
StartLimitBurst=20

[Container]
Image=lscr.io/linuxserver/sonarr:latest
AutoUpdate=registry
ContainerName=sonarr
Network=net-arr.network
Network=net-download.network

# MEASURED ON 2026-08-19, NOT ASSUMED, and the bare form does not work.
# DropCapability=ALL on its own breaks every LinuxServer image outright:
# s6-overlay cannot complete its init without these five, and the container
# never answers. With them it starts clean and still gives up the other nine
# defaults - NET_RAW, MKNOD, SYS_CHROOT, SETPCAP, SETFCAP, KILL, AUDIT_WRITE,
# FSETID and NET_BIND_SERVICE. Proven against radarr and against jellyfin,
# which keeps its GPU through the CDI device with every one of those dropped.
DropCapability=ALL
AddCapability=CHOWN
AddCapability=SETUID
AddCapability=SETGID
AddCapability=DAC_OVERRIDE
AddCapability=FOWNER
NoNewPrivileges=true

Volume=${DOCKER_VOLUME_CONFIG}/sonarr:/config:Z
# One mount, not several: downloads/ and library/ must stay on one filesystem so
# imports can hardlink instead of copying. No :z - see stacks/README.md.
Volume=${DOCKER_VOLUME_MEDIA}:/data

Environment=PUID=0
Environment=PGID=0
Environment=TZ=${TIMEZONE}
Environment=DOCKER_MODS=ghcr.io/gilbn/theme.park:sonarr
Environment=TP_THEME=${TP_THEME}
Environment=TP_COMMUNITY_THEME=${TP_COMMUNITY_THEME}

# Without this `podman ps` reports no health at all and a dead backend looks
# identical to a live one - which is how qBittorrent stayed down for hours.
HealthCmd=curl -fsS http://localhost:8989/ping || exit 1
HealthInterval=60s
HealthTimeout=5s
HealthStartPeriod=60s
HealthRetries=3
# The startup check is what makes Notify=healthy cheap. Measured on Bazarr: with
# only the 60s HealthInterval the first check does not run until t=60s, so every
# unit took ~65s to start. A 5s startup check that stops at the first success
# brought that to 20s - the service's real readiness - while leaving the 60s
# steady-state interval alone, which is what keeps podman's per-check
# health_status events out of the journal.
HealthStartupCmd=curl -fsS http://localhost:8989/ping || exit 1
HealthStartupInterval=5s
HealthStartupRetries=180
HealthStartupSuccess=1
# WITHOUT THIS THE ROLLBACK IS DECORATIVE. podman auto-update rolls back only if
# the unit fails to START, and by default systemd calls a container started the
# moment it is running - so an image that comes up and is broken passes, and the
# old one is never restored. Notify=healthy makes the unit's start depend on the
# healthcheck, which is what turns the rollback into a real one.
Notify=healthy

[Service]
EnvironmentFile=/var/home-server/.env

# Custom scripts are TRACKED IN GIT at apps/sonarr/scripts/ and copied in on
# every start, the same contract as Tdarr's plugins: config/ is gitignored
# runtime state, so git is authoritative and editing the copy on the server is
# pointless, it is overwritten here. The trailing /. copies the directory
# CONTENTS - systemd runs this without a shell, so a glob would not expand.
#
# THE CONTAINER-SIDE PATH IS LOAD-BEARING. Sonarr's "Clean Anime Extra Files"
# Custom Script connection stores /config/scripts/anime-extra-files.sh in
# sonarr.db, which is not in this repository - so a `git grep` does not find it
# and a restore brings the old value back. Where the file lands in the container
# must not change; where it lives in git is free.
ExecStartPre=/usr/bin/mkdir -p ${DOCKER_VOLUME_CONFIG}/sonarr/scripts
ExecStartPre=/usr/bin/cp -a /var/home-server/apps/sonarr/scripts/. ${DOCKER_VOLUME_CONFIG}/sonarr/scripts/

Restart=always
RestartSec=5
# Notify=healthy makes the unit's start block on the healthcheck, and the
# startup check can run for HealthStartupRetries x HealthStartupInterval = 900s.
# systemd's default start timeout is 90s, which would kill a slow but perfectly
# healthy service - so this has to clear 900s with room to spare.
#
# 900s AND NOT 300s, AND ONLY ON THE TEN QUADLETS THAT CARRY A DATABASE - bazarr,
# jellyfin, jellyseerr, ntfy, pocket-id, prowlarr, radarr, sonarr, tdarr-server,
# tinyauth. The list is not a judgement call: it is what bin/snapshot-databases.sh
# finds when it walks config/ by magic bytes.
#
# THE 300s CEILING BRICKED POCKET ID TWICE. Its new image starts, migrates the
# schema, and is still migrating when the gate expires. Notify=healthy never
# fires, podman auto-update rolls the image back - and the OLD image then refuses
# to open the database the new one has already migrated: "failed to run
# migrations: database version (20260814120000) is newer than application".
# Restart=always then loops it. 9.5 hours with no sign-on on 2026-08-18, and
# 9h35m and 6,224 restarts on 2026-08-19, the night after the first fix landed.
#
# THE GATE HAS TO OUTLAST THE MIGRATION, because once it expires the irreversible
# work is already done and the rollback has stopped being a safety net and become
# the outage. This budget is free in normal use - a cold start with no migration
# is ~1s, measured on pocket-id - and exists entirely for the one start after an
# image bump. The stateless services keep 300s: they have nothing to migrate.
TimeoutStartSec=1200

# A ceiling, not a tuning knob: high enough never to bite in normal use,
# low enough that a runaway is killed instead of taking the host down.
MemoryHigh=1G
MemoryMax=1536M

[Install]
WantedBy=default.target
