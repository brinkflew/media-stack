# Everything under config/ is runtime state living on the server - application
# databases, Jellyfin metadata, TLS private keys, session stores. None of it is
# reproducible from this repository, so it is ignored wholesale.
#
# Nothing is re-included by exception any more, and nothing should be. Files
# that need to reach a container's config tree live in apps/<service>/ and are
# copied in by an ExecStartPre= on that service's quadlet - Tdarr's plugin,
# Sonarr's scripts, Jellyfin's custom CSS. That keeps git authoritative without
# tracking a path inside an ignored tree, which needed a four-rule un-ignore
# chain because git will not descend into an ignored directory to find an
# exception inside it.
config/

# Ignore the environment file containing sensitive values.
# The wildcard matters as much as the exact name: editing .env tends to leave
# .env.bak, .env.orig or .env.20260811 next to it, and a bare `.env` rule does
# not match any of those. This repository is public, so an unignored copy is a
# credential leak waiting for one `git add -A`.
#
# .env is generated from secrets/env.sops.env by bin/render-env.sh, so the
# encrypted original IS tracked - its basename does not begin with .env, which
# is why the rules below do not catch it. Check with `git check-ignore -v`
# before assuming, and never rename it to something like secrets/.env.sops.
.env
.env.*
!.env.sample

# age private keys. These live at ~/.config/sops/age/keys.txt and have no
# business in a working tree, but a stray copy here would decrypt every secret
# in the repository it sits in.
*.agekey
keys.txt

# Transcode scratch (DOCKER_VOLUME_CACHE), regenerable and large.
cache/

# Python bytecode. bin/promote-transcoded.py is executed directly, so this is
# only ever produced by importing it - but a .pyc was committed once already.
__pycache__/
*.pyc
